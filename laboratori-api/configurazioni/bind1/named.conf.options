options {
    directory "/var/cache/bind";

    forwarders {
        2001:4860:4860::8888;
    };

    allow-query { ::1; 2a04:0::/56;};
    recursion yes;
    listen-on { ::1; 2a04:0:0:0::4; };
};

logging {
        channel default_syslog {
        syslog local6;
        severity info;
        print-time yes;
    };
    category default { default_syslog; };
    category queries { default_syslog; };
    category security { default_syslog; };
};