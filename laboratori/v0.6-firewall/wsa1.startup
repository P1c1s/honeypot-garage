ip address add 192.169.0.2/24 dev eth0

# a default router is selected
ip route add default via 192.169.0.1

# start web server daemon
# systemctl start apache2
service apache2 start

#soluzione mometanea:

echo "\$dbserver = 'mdb';" >> /etc/phpmyadmin/config-db.php

# start ssh server
service ssh start
useradd -m -p $(perl -e 'print crypt($ARGV[0], "password")' 'senior') senior
useradd -m -p $(perl -e 'print crypt($ARGV[0], "password")' 'junior') junior

/shared/./up.sh