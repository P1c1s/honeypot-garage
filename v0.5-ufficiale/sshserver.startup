ip address add 192.168.0.4/24 dev eth0

# a default router is selected
ip route add default via 192.168.0.1

# custom
systemctl start ssh
useradd -m -p $(perl -e 'print crypt($ARGV[0], "password")' 'admin') admin

/shared/./up.sh